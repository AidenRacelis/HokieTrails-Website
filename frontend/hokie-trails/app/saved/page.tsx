import { NavBar } from "@/components/menu-bar";
const saved = () => {

    return(
        <main>
            <h1>Saved</h1>
            <NavBar></NavBar>
        </main>
        
    )
}

export default saved;
